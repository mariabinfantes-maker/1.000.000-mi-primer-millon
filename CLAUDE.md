# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository (`1.000.000-mi-primer-millon`) is currently **empty**: no commits, no source files, and no remote branches other than the one created for this documentation task. There is no build system, test suite, dependency manifest, or application code yet.

Because there is nothing to build, lint, or test, this file intentionally does not include those sections. Once real code is added, update this file to describe:

- Actual build/lint/test commands (and how to run a single test)
- The high-level architecture and how major components relate
- Project-specific conventions that aren't obvious from the code itself

## Notes for future work

- The name suggests a personal-finance / "first million" themed project, but no framework, language, or structure has been chosen yet — do not assume a stack.
- When the first real code lands, replace this file's content with accurate, codebase-derived documentation rather than extending these placeholder notes.
